# ──────────────────────────────────────────
# Data Science Libraries
# ──────────────────────────────────────────

import numpy as np                # Numerical computing with n-dimensional arrays
import scipy                       # Scientific computing (optimization, stats, signal processing)
import matplotlib.pyplot as plt   # 2D plotting & visualization library
import seaborn as sns             # Statistical data visualization built on Matplotlib
import scikit_learn as skl        # Machine learning algorithms & utilities
import statsmodels.api as sm      # Statistical modeling & econometrics
import tensorflow as tf           # Deep learning framework (neural network building)
import torch                       # PyTorch deep learning framework
import xgboost                     # Optimized gradient boosting library
import lightgbm                    # Fast, distributed, high-performance gradient boosting
import plotly.express as px       # Interactive, web-based data visualization
import shap                        # Model explanation & interpretability (SHAP values)
import mlflow                      # Experiment tracking & model registry
