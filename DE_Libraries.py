!pip install apache_beam
!pip install fastparquet
# ──────────────────────────────────────────
# Data Engineering Libraries
# ──────────────────────────────────────────

import pandas as pd               # High-performance data structures & data analysis tools
import sqlalchemy                 # Database toolkit & ORM for interacting with SQL databases
import pyarrow                    # Fast columnar in-memory analytics and Apache Arrow integration
import apache_beam as beam        # Unified model for defining & executing data processing pipelines
import airflow                    # Workflow orchestration & scheduling (Apache Airflow)
import kafka                      # Apache Kafka client for real-time data streaming
import boto3                      # AWS SDK for Python (S3, Redshift, Glue, etc.)
import google.cloud.bigquery as bq # Google BigQuery client library
